import { ScheduleScreen } from '@/features/schedule';

/**
 * The Schedule tab — the rehearsal agenda. Logic lives in the schedule feature;
 * this route just mounts the screen.
 */
export default function Schedule() {
  return <ScheduleScreen />;
}
