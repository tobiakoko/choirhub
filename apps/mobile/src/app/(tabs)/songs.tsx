import { SongsScreen } from '@/features/songs';

/**
 * The Songs tab — the offline repertoire library. All logic lives in the songs
 * feature; this route just mounts the screen.
 */
export default function Songs() {
  return <SongsScreen />;
}
